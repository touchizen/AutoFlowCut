# Shopping M2b-3 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Wire shopping plan drafting to a real Gemini JSON-mode adapter with main-owned product grounding, key recovery UX, usage tracking, and two carried regression fixes.

**Architecture:** `planMachine` supplies a non-overridable crawl `planContext`; `createGeneratePlan` sanitizes and validates all model inputs and grounds the returned draft; a dedicated Gemini adapter builds a complete prompt-only JSON contract and calls schema-less Gemini JSON mode. The main process injects the completed generator into shopping IPC while retaining M3/M4 stubs.

**Tech Stack:** Electron main IPC, Gemini Generative Language REST API, React, Vitest, Testing Library, Vite.

---

### Task 1: Prompt-shape contract and Gemini JSON-mode usage metadata

**Files:**
- Modify: `electron/shopping/planSchema.js`
- Modify: `electron/api/llm/llmGemini.js`
- Test: `tests/electron/shopping/planSchema.test.js`
- Test: `tests/electron/api/llm/llmGemini.test.js`

1. Add failing tests for exact crawl schema keys, required fields, enums/type nodes, and nullable scene trim; preserve it as a prompt contract.
2. Add failing JSON-mode tests proving no API schema is sent and candidate/thinking `usageMetadata` plus retry deltas reach an injected usage sink.
3. Run the focused tests and confirm missing exports/usage callbacks fail.
4. Derive and export the preserved schema from shared `planSchema` contract constants.
5. Export schema-less `jsonModeCall` while leaving Story `structuredCall` unchanged; report prompt/candidate/thinking token counts for every received response.
6. Rerun the focused tests to green.

### Task 2: Gemini shopping adapter

**Files:**
- Create: `electron/shopping/shoppingLlmGemini.js`
- Create: `tests/electron/shopping/shoppingLlmGemini.test.js`

1. Add failing tests for facts/assets/A-B/context prompt inclusion, Flash selection, prompt-only shape/enums/relational rules, abort forwarding, strict draft return, missing-key code, no key leakage, and usage accumulation.
2. Run the new suite and confirm the module/API is absent.
3. Implement the minimal adapter around injected `jsonModeCall`, `getApiKey`, and `usageTracker`; keep Story `structuredCall` unchanged.
4. Rerun to green and refactor prompt construction while green.

### Task 3: Main-owned plan context and returned-draft grounding

**Files:**
- Modify: `electron/shopping/planMachine.js`
- Modify: `electron/shopping/generatePlan.js`
- Test: `tests/electron/shopping/generatePlan.test.js`
- Test: `tests/electron/shopping/planMachine.test.js`

1. Add failing tests proving caller context is overridden by the crawl snapshot and reaches the LLM only after sanitization.
2. Add failing tests for missing/mismatched context, invented scene/product image IDs, product-reference drift, product-summary/source-fact drift, invented price claims, non-numeric fake brands in claims/scene copy, and exact product-identity grounding.
3. Run the focused suites and confirm the new gates are absent.
4. Implement context construction, sanitization, pre-call grounding, returned product projection equality, scene image grounding, controlled fact/template copy, controlled production copy, and product-identity validation.
5. Preserve the dedicated missing-key code in `draftPlan`.
6. Rerun focused suites to green.

### Task 4: IPC/main DI and key UX

**Files:**
- Modify: `electron/ipc/shopping-api.js`
- Modify: `electron/main.js`
- Modify: `src/components/shopping/ShoppingPanel.jsx`
- Test: `tests/electron/ipc/shopping-api.test.js`
- Test: `tests/electron/ipc/shopping-main-wiring.test.js`
- Test: `tests/components/shopping/ShoppingPanel.test.jsx`

1. Add failing tests proving provided `generatePlan` is installed in machine deps and main creates/injects the Gemini adapter plus `createGeneratePlan`.
2. Add a failing UI test for the inline `shopping-llm-key-missing` settings instruction.
3. Run the focused tests and confirm the stub/default message fails them.
4. Add the DI parameter, main wiring through `genaiKeyStore`, and inline error mapping.
5. Rerun to green.

### Task 5: MINOR regression fixes

**Files:**
- Modify: `electron/shopping/planMachine.js`
- Modify: `src/components/shopping/ShoppingPanel.jsx`
- Test: `tests/electron/shopping/planMachine.test.js`
- Test: `tests/components/shopping/ShoppingPanel.test.jsx`

1. Add a failing remount test where durable pending materialization exists but transient error is absent.
2. Add a failing plan-review merge test where a fact decision references an unknown crawl fact ID.
3. Run both focused suites and confirm the retry is hidden and the forged merge changes state/hash.
4. Derive retry from durable pending state and cross-check incoming IDs before any revision mutation.
5. Rerun to green.

### Task 6: Verification and review

**Files:**
- Inspect every modified/new file.

1. Run all focused shopping/LLM/IPC/UI suites.
2. Run `git diff --check`.
3. Run `npm run test:run` and inspect complete results.
4. Run `npm run build` and inspect the exit code.
5. Apply the verification-before-completion and requesting-code-review checklists without committing.

No commit step is included because the user explicitly prohibited commits.
