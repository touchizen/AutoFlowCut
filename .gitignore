node_modules/
dist/
dist-electron/
output/
release/
.DS_Store
*.log
.superpowers/
.env
.env.*
!.env.example
client_secret_*.json
AutoFlow_10.7.58/
AutoFlow_10.7.58_pretty/
AutoFlow_10.7.58.zip
package-lock.json
.claude/

# Python build artifacts
__pycache__/
*.pyc
*.pyo

# Large ML model binaries (depth/rembg/inpaint) — >100MB, never commit; distribute separately
_models/
*.onnx

# 내부 개발 문서 (public 미공개 — disk-only)
# docs/plans-archive/ 는 추적한다 — CLAUDE.md 가 "완료된 plan/spec 은 여기로 옮기고 commit" 을
# 지시하는데, 무시하면 그 커밋이 아카이브가 아니라 삭제가 된다.
docs/superpowers/
